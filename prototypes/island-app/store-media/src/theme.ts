/* the app's own palette (styles.css :root) and mark (ui/brand.ts) — store
   media and the app should never disagree on either */
export const COLOR = {
  cream: "#F2ECEB", creamDeep: "#E6DCDE", card: "#FCFAF9",
  ink: "#2B2230", muted: "#8E8090", berry: "#9C4F76", berryDeep: "#7C3D60",
  gold: "#DFA23A", plum: "#3A2640", line: "#E2D7DA",
};
export const DISPLAY = "'Fraunces', Georgia, serif";
export const UI = "'Nunito', system-ui, sans-serif";
